# sponsorbot-assets

Status icons for the sponsorship bot's Discord status card: green check (ok), orange clock (waiting), red exclamation (problem). The bot references them by raw URL (`STATUS_ICON_BASE`); this repo is public only so Discord can fetch them.
